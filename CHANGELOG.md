# 更新日志

本文件记录飞白 Inkwhite 的版本演进。版本号遵循语义化版本。

## [未发布]

- HTML 导出保留预览外层容器、当前字体/字号/行高和纸色，内联内置正文字体；修复重开文件后样式失效，移除代码复制按钮。
- Word 导出从预览捕获字体/字号/颜色/段落样式，保留下划线、删除线、高亮、上下标、外链、列表层级和表格段落；移除额外文件名标题和代码复制按钮。
- PDF 打印的「跟随预览」纸色选项现实际生效，保留预览文字颜色、暗纸图表原色；未选时仍使用白纸模式。
- 已验证生成文件的结构和浏览器样式；Word 字体未嵌入，复杂布局、分页、语法配色仍受 Word 排版能力限制，不保证逐像素一致。

## [1.2.5] - 2026-09-17

- 「菜单字号」升级为「界面字号」，顶部菜单与底部状态栏文字、字体选择器、字号按钮同步调整；大字号底栏支持换行。
- 底部源码/预览两组字号合并为一组「正文」，字体与字号同时控制源码、预览及搜索高亮层，沉浸阅读字号按钮也同步。
- 默认字体两侧统一使用楷体；旧独立字号优先保留预览阅读字号，旧界面字号设置继续兼容。
- 修复沉浸阅读顶部菜单遮挡字号按钮的问题。

## [1.2.4] - 2026-09-17

- 新增「主题 → 设置 → 菜单字号」：12–24 px 逐级可选，默认 16 px，即时应用、自动保存并支持恢复默认。
- 顶部菜单、下拉菜单和右键菜单独立调节，不改变正文、源码字号与版心宽度。
- 大字号菜单支持纵向滚动，窄窗口顶部菜单换行，避免下拉条目被裁切。
- 修复打开文件树后视图菜单未关闭、遮挡侧边栏收起按钮的问题。
- 修复窄屏菜单换行后左侧越界：按当前视口动态调整位置和可用高度，窗口缩放时重新定位。

## [1.2.3] - 2026-08-23

- 品牌一致性修复：可执行文件已为 Inkwhite.exe，同步重命名 favicon（symark-lightning-animated.svg → inkwhite-lightning-animated.svg）、更新 package.json 包名与构建脚本注释。

## [1.2.2] - 2026-08-23

- 修复安装后显示为 symark.exe 的问题：可执行文件重命名为 Inkwhite.exe（与产品名一致）。

## [1.2.1] - 2026-08-23

- 优化语法大全：新增「常用」置顶（高频 8 项一键直达）、搜索关键词高亮、分类与搜索联动优化；
- 快捷键：新增 `Ctrl+Shift+H` 直达语法大全；
- 字体与预览：补齐快捷键提示（粗体 Ctrl+B / 斜体 Ctrl+I / 引用 Ctrl+Q 等），Emoji 扩展至 20+ 常用表情；
- 插入体验：预览模式下插入后自动切分屏，插入模板占位词自动选中。

## [1.2.0] - 2026-08-23

新增「语法大全」——面向偶尔写作、记不住语法的场景（参考 https://www.cnblogs.com/miki-peng/articles/12502985.html 并适配本项目全部扩展）：

- **帮助 → 语法大全**：分组卡片展示 40+ 常用语法（标题、文本样式、列表、引用、代码、链接、图片、表格、分割线、脚注、目录、数学公式、任务列表、Emoji、Front Matter、Mermaid 等）；
- **搜索与分类筛选**：输入标题/分类/语法即时过滤；左侧分类一键切换；
- **一键插入与复制**：点击卡片或“插入”按钮将模板插入光标处（自动选中占位词便于直接输入），“复制”按钮带反馈；预览模式下插入后自动切分屏便于查看效果；
- **所见即所得预览**：每张卡片右侧以与主预览一致的渲染管线实时展示效果；
- 交互：遮罩点击/ESC 关闭；小窗自适应单列。

## [1.1.0] - 2026-08-22

本版本围绕「查看型定位」完成阅读体验重构，并通过对抗性验证加固了安全与稳定性。

### 新增
- **阅读模式**：启动即进入预览视图，并记忆上次的视图模式（编辑 / 分屏 / 预览），重启精确还原。
- **阅读位置记忆**：按文档自动记住每篇读到的位置；重新打开、切换标签、外部编辑器改动后重载，都会回到原处（最多保留最近 300 篇）。
- **版心居中**：预览内容采用 Typora 式居中排版（820px 版心，左右留白；全屏宽幅模式 1240px 不受限；打印铺满页宽）。
- **数据安全备份**：保存、自动写穿、另存为覆盖磁盘上已有文件前，自动在同目录留一份 .bak 备份（单份滚动，内容未变不重复写入，备份失败不阻塞保存）。
- **免安装版**：新增便携 zip 分发，解压即用（需系统已具备 WebView2 运行时，Win10/11 默认自带）。
- **持续集成**：GitHub Actions windows-latest 全链守门——代码体积门禁、TypeScript、阅读字体引导、前端单测、Rust 测试、构建、Playwright E2E。

### 修复
- **启动体验三连修**：消除首帧布局跳变与内容双渲染（隐藏启动 + 就绪即显）；修复隐藏窗口中等待 rAF 导致显示永久挂起（每次启动多等约 5 秒）；白屏/黑屏底色一并治理。
- **阅读字体静默缺失**：修复字体资源缺失导致全文回退系统楷体且无任何提示的问题；现随安装包分发并在页面预加载。
- **「更多格式 ⋯」无反应**：下拉浮层被工具栏 overflow 裁剪不可见，改为固定定位锚定按钮下方。
- **外部协作断点**：外部编辑器修改文件后自动重载会跳回顶部，现恢复到原阅读位置。
- **菜单下拉不关闭**：视图/主题/纸色/段落标题等 17 个菜单动作执行后浮层滞留，统一修正。
- 标签页切换时同步本地路径，避免阅读位置键错乱。

### 安全（对抗性验证）
- **ReDoS**：恶意构造的未闭合批注标签可使清洗正则 O(n²) 回溯（约 50 万字符冻结 UI 数秒），已改线性扫描器。
- **渲染进程崩溃**：200 层连续引用链导致 Chromium 构建超深 DOM 时崩溃，超深引用/缩进自动降级为代码块原样展示。
- **净化器加固**：显式禁绝 form/base/noscript 等只读阅读无合法用途的标签（此前 DOMPurify 默认放行 form）。
- 新增对抗性测试套件：XSS 弹药库、mXSS 探针、结构炸弹、存储投毒、原型污染、敌意标题等 12 例持续守门。

### 变更
- WebView2Loader.dll 作为正式资源入库，克隆仓库后无需手工拷贝即可构建。
- 纸色色板单一源化（tokens.css 统一变量）。
- 测试规模：前端单测 465 个、Rust 测试 59 个、E2E 160 个全绿。

## [1.0.0] - 2026-08-19

首个公开版本：Typora 风格菜单栏、多文档标签页、四上下文右键菜单、源码工具栏、统一字体（含导入）、侧边栏文件树与大纲、搜索替换（源码/预览双路由）、划线批注、HTML/PDF/Word/长图导出、本地文件双向同步、沉浸式阅读（五档纸色）、墨笺暗色主题与草书品牌标识。